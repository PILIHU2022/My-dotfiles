#!/usr/bin/env zsh

function exa {
  local bindir="/home/PILIHU/.config/zsh.d/zi.d/zi/plugins/ogham---exa/bin"
  
  
  
  local -xU PATH="$bindir":"$PATH"
  
      "$bindir"/"exa" "$@"

  }

exa "$@"
