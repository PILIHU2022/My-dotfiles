#!/usr/bin/env zsh

function fzy-tmux {
  local bindir="/home/PILIHU/.config/zsh.d/zi.d/zi/plugins/fzy/contrib"
  
  
  
  local -xU PATH="$bindir":"$PATH"
  
      "$bindir"/"fzy-tmux" "$@"

  }

fzy-tmux "$@"
