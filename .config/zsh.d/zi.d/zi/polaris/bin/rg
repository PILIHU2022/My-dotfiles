#!/usr/bin/env zsh

function rg {
  local bindir="/home/PILIHU/.config/zsh.d/zi.d/zi/plugins/BurntSushi---ripgrep/ripgrep"
  
  
  
  local -xU PATH="$bindir":"$PATH"
  
      "$bindir"/"rg" "$@"

  }

rg "$@"
