#!/usr/bin/env zsh

function sk {
  local bindir="/home/PILIHU/.config/zsh.d/zi.d/zi/plugins/lotabout---skim/bin"
  
  
  
  local -xU PATH="$bindir":"$PATH"
  
      "$bindir"/"sk" "$@"

  }

sk "$@"
