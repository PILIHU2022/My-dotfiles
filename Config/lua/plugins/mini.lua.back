return {
    'echasnovski/mini.nvim',
    version = false,
    lazy = true,
    require('mini.surround').setup(),
        config = function ()
            mappings = {
            comment = 'gc',
            comment_line = 'gcc',
            comment_visual = 'gc',
            textobject = 'gc',
            }
        end,
}
